# Backend Environment Variables for Render
# Copy these to your Render Web Service environment variables

# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration (Render PostgreSQL)
# Render provides this automatically when you add a PostgreSQL database
# Format: postgresql://user:password@host:port/database
DATABASE_URL=${RENDER_POSTGRES_CONNECTION_STRING}

# API Configuration
API_BASE_URL=https://api.data.gov.in

# CORS Configuration
# Add your Vercel frontend URL
ALLOWED_ORIGINS=https://your-app.vercel.app,https://your-custom-domain.com

# Optional: Additional CORS origins for preview deployments
# ALLOWED_ORIGINS=https://your-app.vercel.app,https://your-app-*.vercel.app,https://your-custom-domain.com
